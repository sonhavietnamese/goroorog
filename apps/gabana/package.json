{
  "name": "gabana",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --outfile gabana --target=bun",
    "start": "./gabana"
  },
  "dependencies": {
    "@elysiajs/cron": "^1.3.0",
    "elysia": "latest",
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
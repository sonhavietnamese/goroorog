export const SEEDS = {
  PLAYERS: Buffer.from('players'),
  BOSS: Buffer.from('boss'),
  SKILLS: Buffer.from('skills'),
  STATS: Buffer.from('stats'),
  HISTORY: Buffer.from('history'),
  RESOURCES: Buffer.from('resources'),
}
